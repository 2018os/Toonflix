const fontSizes = {
  smallest : '12px',
  small : '16px',
  default : '18px',
  large : '24px',
  larger : '36px',
  largest : '48px',
  h1 : '48px',
  h2 : '36px',
  h3 : '24px',
};

const colors = {
  primaryColor: '#5e74ff',
  gray: '#EAEDED',
  black: '#212121',
};

const textColors = {
  primaryColor: '#5e74ff',
  gray: '#7A7A7A',
  black: '#212121',
};

const theme = {
  // colors
  colors,
  textColors,
  // fontSizes
  fontSizes,
  // spaces
};

export default theme;
fragment comment on Comment {
  id
  message
  createdAt
  writer {
    name
  }
}

fragment commentsConnectionForComments on CommentsConnection {
  pageInfo {
    hasNextPage
    endCursor
  }
  edges {
    node {
      ...comment
    }
  }
}
